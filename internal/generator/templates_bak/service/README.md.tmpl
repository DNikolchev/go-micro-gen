# {{.ServiceName}}

> Production-ready Go microservice generated by [go-micro-gen](https://github.com/go-micro-gen/go-micro-gen)

## Stack

| Layer | Technology |
|---|---|
| Language | Go {{.GoVersion}} |
| Router | go-chi/chi |
| Database | {{.Database}} |
| Observability | OpenTelemetry + Prometheus + Grafana |
| Container | Docker (multi-stage, distroless) |
| CI/CD | {{.CI}} |

## Quick Start

```bash
# Copy env configuration
cp .env.example .env

# Start all services (App + DB + Monitoring)
make up

# Check health
curl http://localhost:8080/health
```

## Available Commands

```bash
make build         # Build the binary
make run           # Run locally
make test          # Run tests with race detector
make test-coverage # Run tests + generate coverage.html
make lint          # Run golangci-lint
make fmt           # Format code
make up            # docker compose up -d
make down          # docker compose down
make logs          # Tail application logs
make clean         # Remove artifacts
```

## Service Endpoints

| Endpoint | Description |
|---|---|
| `GET /health` | Liveness probe |
| `GET /ready` | Readiness probe |
| `POST /api/v1/examples` | Create entity |
| `GET /api/v1/examples` | List entities |
| `GET /api/v1/examples/{id}` | Get by ID |
| `PUT /api/v1/examples/{id}` | Update entity |
| `DELETE /api/v1/examples/{id}` | Delete entity |

## Observability

| Tool | URL |
|---|---|
| Service | http://localhost:8080 |
| Prometheus | http://localhost:9090 |
| Grafana | http://localhost:3000 (admin/admin) |

## Project Structure

```
.
├── cmd/main.go                      # Entrypoint — wires everything
├── internal/
│   ├── config/config.go             # Env-based configuration
│   ├── domain/entity.go             # Domain entities
│   ├── repository/                  # Storage abstraction
│   │   ├── repository.go            # Interface (contract)
│   │   └── {{.Database}}/impl.go   # Implementation
│   ├── service/service.go           # Business logic
│   └── transport/http/handler.go    # HTTP handlers + routing
├── pkg/
│   ├── telemetry/otel.go            # OpenTelemetry setup
│   ├── logger/logger.go             # Structured JSON logger
│   └── health/health.go            # Health/readiness endpoints
├── docker/
│   ├── Dockerfile                   # Multi-stage build
│   ├── docker-compose.yml           # Full dev/staging stack
│   └── configs/                     # OTEL + Prometheus configs
├── .golangci.yml                    # Linter configuration
├── .env.example                     # Environment template
├── Makefile                         # Standard dev commands
└── README.md
```

## Architecture: {{.Architecture}}

This service follows **{{.Architecture}} architecture** principles:
- All dependencies point inward (domain has no external deps)
- Business logic (`service/`) is completely DB-agnostic
- The `repository.Repository` interface decouples storage from logic
- Transport layer (`transport/http/`) only calls the service, never the DB

---

*Generated by `go-micro-gen` — The Ultimate Microservice Scaffolder*
