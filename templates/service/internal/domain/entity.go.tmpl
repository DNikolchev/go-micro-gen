package domain

import "time"

// Example is the core domain entity for {{.ServiceName}}.
// Replace this with your actual business entity.
type Example struct {
	ID        string    `json:"id"`
	Name      string    `json:"name"`
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
}
